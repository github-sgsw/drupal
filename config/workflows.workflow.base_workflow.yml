uuid: e6669ab8-5e56-44e1-8888-eaca40a0a44d
langcode: ja
status: true
dependencies:
  config:
    - media.type.file
    - node.type.news
  module:
    - content_moderation
id: base_workflow
label: 基本ワークフロー
type: content_moderation
type_settings:
  states:
    draft:
      label: draft
      weight: 0
      published: false
      default_revision: false
    pending_approval:
      label: pending_approval
      weight: 2
      published: false
      default_revision: false
    published:
      label: published
      weight: 1
      published: true
      default_revision: true
    reject_approval:
      label: reject_approval
      weight: 6
      published: false
      default_revision: false
    un_published:
      label: 'un published'
      weight: 5
      published: false
      default_revision: true
  transitions:
    default_draft:
      label: draft
      from:
        - draft
        - pending_approval
        - published
        - reject_approval
        - un_published
      to: draft
      weight: 4
    pending_approval:
      label: pending_approval
      from:
        - draft
        - reject_approval
      to: pending_approval
      weight: 5
    published:
      label: published
      from:
        - draft
        - pending_approval
        - published
        - un_published
      to: published
      weight: 0
    reject_approval:
      label: reject_approval
      from:
        - pending_approval
      to: reject_approval
      weight: 6
    un_published:
      label: 'un published'
      from:
        - draft
        - published
      to: un_published
      weight: 1
  entity_types:
    media:
      - file
    node:
      - news
  default_moderation_state: draft
